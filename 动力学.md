$M(q)\ddot{q} + n(q,\dot{q}) = Bu$

这是机器人的动力学方程，用来描述：
给机器人施加电机力矩以后，机器人将产生多大的加速度。

它是牛顿第二定律 \(F=ma\) 在多自由度机器人系统中的矩阵形式。

可以粗略看为
$$
\underbrace{M(q)}_{\text{等效质量}}
\underbrace{\ddot{q}}_{\text{加速度}}
+
\underbrace{n(q,\dot{q})}_{\text{重力等非线性作用}}
=
\underbrace{Bu}_{\text{电机产生的广义力}}
$$

\(q\) 称为广义坐标，表示机器人当前“处在什么姿态和位置”

q上一点：广义速度
它是对q求一次时间导数得来
q上两点：广义加速度


雅可比矩阵：

这两个矩阵描述的是：
当状态 \(x\) 或控制输入 \(u\) 发生一点点变化时，机器人状态变化率 \(f(x,u)\) 会改变多少。

它们叫作雅可比矩阵，本质上就是“多变量函数的一阶导数”。
